[user]
  name = Torsten Trautwein
  email = trautwein@neowork.com
  signingkey = DBC688F05DB0DC29
[push]
  default = simple
  followTags = true
[alias]
  lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
  cleanup = "! /usr/local/bin/git fetch -p && /usr/local/bin/git branch -vv | awk '/: gone]/{print $1}' | xargs /usr/local/bin/git branch -D"
[filter "media"]
  clean = git-media-clean %f
  smudge = git-media-smudge %f
[mergetool]
  keepBackup = true
[pull]
  rebase = true
[filter "lfs"]
  clean = git-lfs clean %f
  smudge = git-lfs smudge %f
  required = true
[core]
  editor = vim -f
  excludesfile = ~/.gitignore
[commit]
  gpgsign = true
[gpg]
  program = /usr/local/bin/gpg
